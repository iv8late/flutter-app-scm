import 'package:flutter/material.dart';
import '../widget/widget_404.dart';

class PageNotFound extends StatelessWidget {
  const PageNotFound({super.key});

  @override
  Widget build(BuildContext context) {
    return drawerWidget404(context);
  }
}
